(i+3=3,1+i)
where
  var i:integer
end

#######################

((ord('A')=1),
 p.x=3,pa^[3]=4,@pa^,pa=null,
 pa^=1,
 @i=null)
  where
      fun ord(c:char):ptr integer=@[integer]c
      var i:integer
      var p:rec{x:integer,y:integer}
      var pa:ptr arr[5]integer
  end

(pi^=3,
 i=3)
where
  var pi:ptr integer
  var i:integer
end

1*3*5=4+p.x+i[3*8*i[4]]
where
  var i:arr[2]integer
  var p:rec{x:integer,y:integer}
end


i
where
  typ int:integer
  var i:arr[4]int
  fun ord(c:char):int=i
end


1+i+ii+p.x+p.y[3]+i
where
  var p:point
  typ point:rec{x:integer,y:arr[5]int}
  typ int:integer
  var ii:integer
  var i:int
  typ a:arr[10+1]int
end


1+(-42) where typ int:integer end



1+[k]i+(ord(j) where var j:integer end)+point.i
where
  var point:rec{i:integer,j:integer}
  var i:integer
  var j:integer
  typ k:integer
  fun ord(c:char):integer=(c+1,for i=1,2:i+2 end)
end

1+2+i where var i:integer end



(a where var a:integer) = 3
